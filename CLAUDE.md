# 全局通用配置｜DeepSeek主模型 + MiniMax图像专用MCP
## 1.模型路由硬性规则（最高优先级）
1. 默认文本、代码编写、调试、需求分析、文档：**强制使用DeepSeek(deepseek-chat)**，不切换模型。
2. 只要出现：上传图片、截图、本地图片附件、图像OCR、图表解析、画面内容识别，**自动调用MiniMax MCP工具**完成图像解析。
3. MiniMax只负责识图，图片解析完成后，结果返回DeepSeek继续整理输出正文，后续对话切回DeepSeek。
4. 不主动切换模型，禁止自行修改ANTHROPIC_MODEL环境变量。

## 2.上下文管理规范
1. 使用 `/context` 查看Token占用，占用≥70%主动提醒执行 `/compact` 压缩会话。
2. `/compact` 压缩时固定保留：模型路由规则、MCP配置、当前项目核心代码逻辑，丢弃调试日志、冗余命令输出。
3. 彻底更换新项目/无关需求时，使用 `/clear` 清空全量上下文。

## 3.MCP工具使用规则
1. MiniMax MCP仅在传入图片时自动唤起，纯文本禁止调用图像MCP。
2. MCP调用失败自动回落：图片无法识别时告知用户，不阻塞主对话。

## 4.输出规范
1. 代码片段带完整注释，区分文件路径；
2. 精简无效客套话术，优先落地代码/方案。
