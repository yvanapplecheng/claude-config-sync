# Machine-specific / ephemeral
backups/
cache/
file-history/
plans/
plugins/
sessions/
tasks/
telemetry/
scripts/
skills/
history.jsonl
settings.json
settings.local.json
.caveman-active
.last-cleanup
.last-update-result.json

# Skills dir excluded — managed via plugin-skill-manifest.json + sync-plugins-skills.ps1
# skills/ is NOT synced; each machine clones per manifest

# Session data inside project dirs — keep only memory/
projects/*/*.jsonl
projects/*/tool-results/
projects/*/subagents/
projects/*/*.meta.json

# Per-machine status files TRACKED for cross-machine comparison dashboard
# status-PCNAME.json is committed+synced via git
!status-*.json
!status.json
